Input the text you wanna test in "TrainData.txt"

Then train the model with "Train_Embeddedmodel.py"

Run the program with WordPrediction.py
It has two options. Predict the remaining word with N-gram or Embedded Model

The result will depend on the text you have in "TrainData.txt" or tune the parameters as your preference in "Train_Embeddedmodel.py"
